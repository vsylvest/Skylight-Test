---
- name: Provision Instances
  hosts: localhost
  gather_facts: False
  tasks:
    - name: get key for skylight
      command: "ls -ltr  /var/lib/awx/projects/"
      register: output

    - name: print files
      debug: 
        var: "{{ output }}"